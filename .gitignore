# FFmpeg binaries (auto-downloaded via build script)
ffmpeg
ffmpeg_downloaded_flag
ffprobe
*ffprobe*
# C# bundled ffmpeg (download manually, not tracked in git)
windows/ffmpeg/ffmpeg.exe

# Build script (not ignored)
# download-ffmpeg.sh is tracked manually

# FFmpeg binaries (download manually for Windows)
ffmpeg/
ffmpeg.exe
ffprobe.exe

# .NET / C# build artifacts
bin/
obj/
*.user
*.suo
.vs/

# macOS
.DS_Store
*.swp
*.swo

# Logs
*.log

# Git worktrees (local only)
macos/
python/

# .NET / C# build artifacts
bin/
obj/
*.user
*.suo
.vs/
